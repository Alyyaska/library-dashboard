# 📚 Library Dashboard

## 📘 Описание проекта

Тестовое **fullstack-приложение для управления библиотекой**.

Приложение реализует работу с авторами и книгами, используя клиент-серверную архитектуру.
Frontend взаимодействует с Backend через REST API, а данные хранятся в PostgreSQL.

---

## ⚙️ Основные возможности

### 📖 Раздел «Библиотека»

#### Авторы

* Infinite Loading (подгрузка через `limit / offset`)
* Выбор автора кликом по строке
* Связь с таблицей книг

#### Книги

* Отображение книг выбранного автора
* Добавление книги
* Редактирование книги
* Удаление книги

---

## 🧩 Технологии

| Компонент        | Назначение                   |
| ---------------- | ---------------------------- |
| **NodeJS**       | Backend-логика               |
| **Express**      | REST API                     |
| **PostgreSQL**   | Хранение данных              |
| **React (Vite)** | Клиентская часть             |
| **AG Grid**      | Табличное отображение данных |

---

## 🗄️ Структура проекта

```
library-dashboard/
├── server/          # Backend (Express + PostgreSQL)
├── client/          # Frontend (React + Vite)
└── init-db.sql      # Скрипт создания БД и тестовых данных
```

---

## 🛠️ Установка и запуск

### 1. Создание базы данных

Из корня проекта выполните:

```powershell
& "C:\Program Files\PostgreSQL\18\bin\psql.exe" -U postgres -f init-db.sql
```

Будет создана:

* база данных `library_dashboard`
* таблицы `authors` и `books`
* тестовые данные

---

### 2. Запуск сервера

Перейдите в папку `server`:

```bash
cd server
npm install
```
Cоздайте файл .env и вставьте:

```bash
PORT=4000
DATABASE_URL=postgresql://postgres:1234@localhost:5432/library_dashboard
```
После этого запустите сервер:

```bash
node src/app.js
```

Сервер будет доступен по адресу:

```
http://localhost:4000
```

---

### 3. Запуск клиента

В новом терминале:

```bash
cd client
npm install
npm run dev
```

Клиент будет доступен по адресу:

```
http://localhost:5173
```

---

## 🔗 REST API

### Authors

```
GET /api/authors?limit=50&offset=0
```

### Books

```
GET    /api/books?authorId=1
POST   /api/books
PUT    /api/books/:id
DELETE /api/books/:id
```

---

## 📌 Особенности реализации

* Связь таблиц через Foreign Key:
  `books.author_id → authors.id`
* Используемые типы данных:
  `INTEGER`, `VARCHAR`, `DATE`, `NUMERIC`
* Infinite Row Model реализован с помощью **AG Grid**
* Архитектура Backend:

  * `models`
  * `controllers`
  * `routes`

---

## ▶️ Порядок запуска

1. PostgreSQL
2. Backend (`server`)
3. Frontend (`client`)

---

📌 После запуска приложение доступно в браузере по адресу:
**[http://localhost:5173](http://localhost:5173)**

